# MCP Server — dependencias
# Solo se necesita numpy (datalayer usa stdlib, websockets para el dashboard).
numpy>=1.26
websockets>=12.0

# Opcional — brokers reales
# ib_insync>=0.9.86   # IBKR (acciones, futuros, opciones)
# python-binance>=1.0.19  # Binance spot/futures real
